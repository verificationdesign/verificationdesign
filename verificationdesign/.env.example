# Optional comma-separated list of card slugs to render.
# Leave unset for local editorial preview of every valid card.
# Example:
# SITE_CARD_ALLOWLIST=constitution,executable-analog,delta,adversarial-frame,comparator,state-baseline,trajectory-cursor
SITE_CARD_ALLOWLIST=
